[package]
name = "hunkle"
version = "0.1.0"
edition = "2024"
description = "Split staged changes into multiple commits, hunk by hunk"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
ratatui = "0.29"

[dev-dependencies]
tempfile = "3"
